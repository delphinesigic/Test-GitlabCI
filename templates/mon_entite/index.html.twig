{% extends 'base.html.twig' %}

{% block title %}MonEntite index{% endblock %}

{% block body %}
    <h1>MonEntite index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Age</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for mon_entite in mon_entites %}
            <tr>
                <td>{{ mon_entite.id }}</td>
                <td>{{ mon_entite.nom }}</td>
                <td>{{ mon_entite.prenom }}</td>
                <td>{{ mon_entite.age }}</td>
                <td>
                    <a href="{{ path('mon_entite_show', {'id': mon_entite.id}) }}">show</a>
                    <a href="{{ path('mon_entite_edit', {'id': mon_entite.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('mon_entite_new') }}">Create new</a>
{% endblock %}

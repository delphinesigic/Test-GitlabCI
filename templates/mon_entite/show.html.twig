{% extends 'base.html.twig' %}

{% block title %}MonEntite{% endblock %}

{% block body %}
    <h1>MonEntite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mon_entite.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ mon_entite.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ mon_entite.prenom }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ mon_entite.age }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mon_entite_index') }}">back to list</a>

    <a href="{{ path('mon_entite_edit', {'id': mon_entite.id}) }}">edit</a>

    {{ include('mon_entite/_delete_form.html.twig') }}
{% endblock %}
